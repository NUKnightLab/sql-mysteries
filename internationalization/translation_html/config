#We'll use the =pybabel-python3= utility to manage GNU Gettext compatible catalog files.
# On Debian, it is in =/usr/bin/pybabel-python3=
# On MacOS, it is pybabel

# il faut installer babel, permet de lister pour vériier que les package sont la
# dpkg est un gestionnaire de paquet, brew sur mac
dpkg -L python3-babel

# ça serait bien de faire un conteneur docker

# Extracting strings from the source
pybabel extract -o mvp.pot mvp.py


# Creating the catalog hierarchy
mkdir -p locale/en/LC_MESSAGES/
mkdir -p locale/fr/LC_MESSAGES/
mkdir -p locale/ch/LC_MESSAGES/

# Initializing .po translation files
cp mvp.pot locale/fr/LC_MESSAGES/fr.po
cp locale/fr/LC_MESSAGES/fr.po locale/ch/LC_MESSAGES/ch.po

# Generating the .mo files
pybabel compile -D fr -l fr -d locale 2>&1

# Translating
python3 translation